pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Đang build project...'
            }
        }

        stage('Test') {
            steps {
                echo 'Đang chạy test...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy xong!'
            }
        }
    }
}
